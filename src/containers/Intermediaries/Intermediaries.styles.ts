import {makeStyles} from '@material-ui/core/styles';

export const intermediariesStyles = makeStyles(() => ({
	tableLoadingProgress: {
		position: 'absolute',
		width: '100%',
	},
	tableContainer: {
		position: 'relative',
	}
}));
